# 项目结构说明 / Project Structure

## 📁 目录结构 / Directory Structure

```
Gemini-Web/
│
├── 📂 src/                          # 前端源代码 Frontend Source
│   ├── 📂 components/               # React 组件
│   │   ├── Avatar/                  # 头像组件
│   │   ├── BasicInfo/               # 基本信息组件
│   │   ├── Billing/                 # 计费组件
│   │   ├── Button/                  # 按钮组件
│   │   ├── ChatContent/             # 聊天内容组件
│   │   ├── ClientOnly/              # 客户端渲染组件
│   │   ├── Footer/                  # 页脚组件
│   │   ├── Header/                  # 页头组件
│   │   ├── Image/                   # 图片组件
│   │   ├── Message/                 # 消息组件
│   │   ├── Scrollbar/               # 滚动条组件
│   │   ├── Setting/                 # 设置组件
│   │   ├── Sidebar/                 # 侧边栏组件
│   │   ├── Text/                    # 文本组件
│   │   └── UserAvatar/              # 用户头像组件
│   │
│   ├── 📂 hooks/                    # 自定义 Hooks
│   │   ├── useChatProgress.ts       # 聊天进度 Hook
│   │   ├── useCountDown.ts          # 倒计时 Hook
│   │   ├── useIsMobile.ts           # 移动端检测 Hook
│   │   ├── useScroll.ts             # 滚动 Hook
│   │   └── useTheme.ts              # 主题 Hook
│   │
│   ├── 📂 pages/                    # 页面
│   │   ├── api/                     # API 路由 (代理)
│   │   │   ├── [...all].ts          # 通用代理
│   │   │   ├── chat-progress.ts     # 聊天进度 API
│   │   │   ├── logout.ts            # 登出 API
│   │   │   └── notice.ts            # 公告 API
│   │   ├── chat/                    # 聊天页面
│   │   ├── login/                   # 登录页面
│   │   ├── _app.tsx                 # App 入口
│   │   ├── _document.tsx            # Document 配置
│   │   └── index.tsx                # 首页
│   │
│   ├── 📂 service/                  # 服务层
│   │   ├── chatgpt.ts               # ChatGPT 服务
│   │   ├── http.ts                  # HTTP 客户端
│   │   ├── localStorage.ts          # 本地存储
│   │   ├── logger.ts                # 日志服务
│   │   └── server.ts                # 服务端工具
│   │
│   ├── 📂 store/                    # 状态管理
│   │   ├── App.tsx                  # 应用状态
│   │   ├── Chat.tsx                 # 聊天状态
│   │   └── User.tsx                 # 用户状态
│   │
│   ├── 📂 styles/                   # 样式文件
│   │   ├── github-markdown.scss     # Markdown 样式
│   │   ├── globals.css              # 全局样式
│   │   ├── highlight.scss           # 代码高亮样式
│   │   └── text.scss                # 文本样式
│   │
│   ├── 📂 utils/                    # 工具函数
│   │   ├── copyToClipboard.ts       # 复制到剪贴板
│   │   ├── downloadAsImage.ts       # 下载为图片
│   │   └── requestAuth.ts           # 请求认证
│   │
│   ├── constants.ts                 # 常量定义
│   └── middleware.ts                # 中间件
│
├── 📂 backend/                      # 后端源代码 Backend Source
│   ├── 📂 src/
│   │   ├── 📂 controllers/          # 控制器层
│   │   │   ├── auth.controller.ts   # 认证控制器
│   │   │   ├── user.controller.ts   # 用户控制器
│   │   │   ├── chat.controller.ts   # 聊天控制器
│   │   │   ├── integral.controller.ts # 积分控制器
│   │   │   └── payment.controller.ts  # 支付控制器
│   │   │
│   │   ├── 📂 services/             # 服务层
│   │   │   ├── prisma.service.ts    # Prisma 数据库服务
│   │   │   ├── redis.service.ts     # Redis 缓存服务
│   │   │   ├── auth.service.ts      # 认证服务
│   │   │   ├── user.service.ts      # 用户服务
│   │   │   ├── email.service.ts     # 邮件服务
│   │   │   ├── openai.service.ts    # OpenAI 服务
│   │   │   ├── integral.service.ts  # 积分服务
│   │   │   └── payment.service.ts   # 支付服务
│   │   │
│   │   ├── 📂 middleware/           # 中间件
│   │   │   ├── auth.middleware.ts   # 认证中间件
│   │   │   ├── error.middleware.ts  # 错误处理中间件
│   │   │   └── rateLimit.middleware.ts # 限流中间件
│   │   │
│   │   ├── 📂 routes/               # 路由
│   │   │   ├── index.ts             # 路由汇总
│   │   │   ├── auth.routes.ts       # 认证路由
│   │   │   ├── user.routes.ts       # 用户路由
│   │   │   ├── chat.routes.ts       # 聊天路由
│   │   │   ├── integral.routes.ts   # 积分路由
│   │   │   └── payment.routes.ts    # 支付路由
│   │   │
│   │   ├── 📂 types/                # 类型定义
│   │   │   └── index.ts             # 类型汇总
│   │   │
│   │   ├── 📂 utils/                # 工具函数
│   │   │   ├── response.util.ts     # 响应工具
│   │   │   ├── jwt.util.ts          # JWT 工具
│   │   │   ├── validator.util.ts    # 验证工具
│   │   │   └── helpers.ts           # 辅助函数
│   │   │
│   │   └── app.ts                   # 应用入口
│   │
│   ├── 📂 prisma/                   # 数据库
│   │   └── schema.prisma            # 数据模型
│   │
│   ├── package.json                 # 后端依赖配置
│   ├── tsconfig.json                # TypeScript 配置
│   ├── Dockerfile                   # Docker 配置
│   └── env.example                  # 环境变量模板
│
├── 📂 public/                       # 静态资源
│   ├── author.jpg                   # 默认头像
│   ├── favicon.ico                  # 网站图标
│   ├── logo.svg                     # Logo
│   └── particles.json               # 粒子效果配置
│
├── 📂 scripts/                      # 部署脚本
│   ├── setup.sh                     # 一键配置 (Linux/Mac)
│   ├── setup.ps1                    # 一键配置 (Windows)
│   ├── deploy.sh                    # 部署管理
│   └── README.md                    # 脚本说明
│
├── 📜 配置文件 Configuration
│   ├── docker-compose.yml           # Docker 完整配置
│   ├── docker-compose.dev.yml       # Docker 开发配置
│   ├── Dockerfile                   # 前端 Docker
│   ├── .dockerignore                # Docker 忽略
│   ├── .gitignore                   # Git 忽略
│   ├── .editorconfig                # 编辑器配置
│   ├── .prettierrc                  # Prettier 配置
│   ├── next.config.js               # Next.js 配置
│   ├── tailwind.config.js           # Tailwind 配置
│   ├── postcss.config.js            # PostCSS 配置
│   ├── tsconfig.json                # TypeScript 配置
│   └── env.example                  # 环境变量模板
│
├── 📜 文档 Documentation
│   ├── README.md                    # 英文文档
│   ├── README-zh.md                 # 中文文档
│   ├── PORTS.md                     # 端口映射文档
│   ├── STRUCTURE.md                 # 项目结构文档 (本文件)
│   └── LICENSE                      # 开源协议
│
└── 📜 入口脚本 Entry Scripts
    ├── setup.sh                     # 快捷部署入口 (Linux/Mac)
    └── setup.bat                    # 快捷部署入口 (Windows)
```

## 🏗️ 架构说明 / Architecture

### 前端架构 Frontend

```
┌─────────────────────────────────────────────────────────┐
│                      Next.js App                        │
├─────────────────────────────────────────────────────────┤
│  Pages          │  Components      │  Hooks             │
│  - index        │  - ChatContent   │  - useChatProgress │
│  - login        │  - Message       │  - useIsMobile     │
│  - chat/[id]    │  - Sidebar       │  - useTheme        │
├─────────────────────────────────────────────────────────┤
│                    Store (Context)                      │
│  - App Context  │  - Chat Context  │  - User Context    │
├─────────────────────────────────────────────────────────┤
│                    Services                             │
│  - HTTP Client  │  - ChatGPT      │  - LocalStorage     │
├─────────────────────────────────────────────────────────┤
│                    API Routes (Proxy)                   │
│  - /api/[...all]  →  Backend API                        │
└─────────────────────────────────────────────────────────┘
```

### 后端架构 Backend

```
┌─────────────────────────────────────────────────────────┐
│                    Express App                          │
├─────────────────────────────────────────────────────────┤
│                    Middleware                           │
│  - CORS         │  - Auth         │  - RateLimit       │
├─────────────────────────────────────────────────────────┤
│                    Routes                               │
│  /api/v1/user   │  /api/v1/openai │  /api/v1/pay       │
├─────────────────────────────────────────────────────────┤
│                    Controllers                          │
│  - Auth         │  - Chat         │  - Payment         │
├─────────────────────────────────────────────────────────┤
│                    Services                             │
│  - AuthService  │  - OpenAIService│  - IntegralService │
├─────────────────────────────────────────────────────────┤
│                    Data Layer                           │
│  - Prisma (PostgreSQL)  │  - Redis (Cache)             │
└─────────────────────────────────────────────────────────┘
```

## 🔄 数据流 / Data Flow

```
┌──────────┐    ┌──────────┐    ┌──────────┐    ┌──────────┐
│  Client  │───▶│ Frontend │───▶│ Backend  │───▶│ Database │
│ (Browser)│    │ (Next.js)│    │ (Express)│    │(Postgres)│
└──────────┘    └──────────┘    └──────────┘    └──────────┘
                     │               │               │
                     │               │               │
                     ▼               ▼               ▼
                ┌──────────┐   ┌──────────┐   ┌──────────┐
                │  State   │   │  Redis   │   │ OpenAI   │
                │ (Context)│   │  Cache   │   │   API    │
                └──────────┘   └──────────┘   └──────────┘
```

## 📝 命名规范 / Naming Conventions

| 类型 | 规范 | 示例 |
|------|------|------|
| 文件夹 | kebab-case | `chat-content` |
| 组件文件 | PascalCase | `ChatContent.tsx` |
| 工具文件 | camelCase | `copyToClipboard.ts` |
| 服务文件 | camelCase.service | `auth.service.ts` |
| 路由文件 | camelCase.routes | `auth.routes.ts` |
| 常量 | UPPER_SNAKE_CASE | `MAX_TOKEN_LENGTH` |
| 变量/函数 | camelCase | `getUserInfo` |
| 类型/接口 | PascalCase | `UserProfile` |

